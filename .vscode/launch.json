{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Buy for Bub",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "env": {
        "PORT": "8080",
        "DB_PATH": "${workspaceFolder}/data/buyforbub.db",
        "APP_TITLE": "Buy for Bub"
      }
    }
  ]
}
